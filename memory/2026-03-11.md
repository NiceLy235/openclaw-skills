# 2026-03-11 Memory

## lerobot 训练流程完整化

### 完成的工作

1. **发现并修复 env-setup skill 安装问题**
   - 问题：之前用 `pip install lerobot==0.4.4` 而不是 `pip install -e .`
   - 影响：无法使用工作目录的脚本
   - 修复：在 192.168.136.128 上重新以可编辑模式安装
   - 已更新 env-setup skill，支持 `REPO_PATH` 参数

2. **完成数据集准备和训练**
   - 数据位置：192.168.136.128 `/home/nice/ly/data/pre_merge_data/2026_03_04_10_06`
   - 15 集数据，已分割为训练集(12集)和验证集(3集)
   - 使用 `lerobot_edit_dataset` 合并为 `ly/train_merged` 和 `ly/val_merged`

3. **SmolVLA 训练已启动**
   - 需要 HuggingFace Token 和代理
   - Token: 查看 `~/.huggingface/token` 或环境变量
   - 代理: `http://127.0.0.1:10809`
   - 训练脚本: `/home/nice/ly/data/training_data/train_smolvla.sh`

4. **更新 lerobot-auto-train skill**
   - 集成完整训练流程
   - `prepare_dataset.py` 支持 split/merge/full 命令
   - `task_manager.py` 支持提交训练任务
   - 已推送到 GitHub

### 关键配置

```bash
# 192.168.136.128 上的配置
conda activate ly_robot
export HTTP_PROXY=http://127.0.0.1:10809
export HTTPS_PROXY=http://127.0.0.1:10809

# 数据集
ly/train_merged (12 episodes, 9817 frames)
ly/val_merged (3 episodes, 2286 frames)

# 训练命令
python -m lerobot.scripts.lerobot_train \
  --policy.pretrained_path=lerobot/smolvla_base \
  --policy.type=smolvla \
  --policy.load_vlm_weights=true \
  --dataset.repo_id=ly/train_merged \
  --output_dir=outputs/mylerobot_train/0311_smolvla \
  --batch_size=32 \
  --steps=100000
```

### SSH 访问

```bash
sshpass -p 'NICE' ssh nice@192.168.136.128
```

### 下次继续

如果训练中断或需要新训练：
```bash
# 使用 lerobot-auto-train skill
cd ~/.openclaw/workspace/skills/lerobot-auto-train/scripts

# 一键流程
python prepare_dataset.py full --data-dir ... --repo-prefix ly --proxy http://127.0.0.1:10809
python task_manager.py submit --dataset-repo-id ly/train_merged --proxy http://127.0.0.1:10809 --hf-token hf_xxx
```
